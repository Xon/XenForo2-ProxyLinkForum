{
    "template": "node_list_forum",
    "description": "Patch links",
    "execution_order": 5845,
    "enabled": true,
    "action": "str_replace",
    "find": "{{ link('forums', $node) }}",
    "replace": "{{ $extras.ProxiedForum ? link('forums', $extras.ProxiedForum) : link('forums', $node) }}"
}